Small application which demonstrates CSV parsing (comma separate values).

The CSV parsing code in jope.foundation.csv is based on Apache commons-csv.

The WOFileUpload element used to upload a CSV file requires the Apache
commons-fileupload-1.2.jar in the CLASSPATH.
